user = input ("ชื่อผู้ใช้: ")
if user == "admin":
    pwd = input ("รหัสผ่าน: ")
    if pwd == "AdminPass" :
        print ("ยินดีต้อนรับ Admin")
    else:
        print ("รหัสผ่านไม่ถูกต้อง")
else:
    print("ไม่มีสิทธิ์เข้าถึง")
